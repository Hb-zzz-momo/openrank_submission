# backend/.env.example
# 复制此文件为 .env 并填入实际值

# Flask 运行模式：development / production
FLASK_ENV=development

# 安全密钥（可用 python -c "import secrets; print(secrets.token_hex(32))" 生成）
SECRET_KEY=your-secret-key-here
JWT_SECRET_KEY=your-jwt-secret-key-here

# CORS 允许的源（生产环境必须配置，多个域名用逗号分隔）
# 示例：ALLOWED_ORIGINS=https://example.com,https://www.example.com
ALLOWED_ORIGINS=

# OpenAI API
OPENAI_API_KEY=sk-xxx